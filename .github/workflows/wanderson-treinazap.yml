name: Deploy-TREINAZAP S3
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v1
        with:
          node-version: 12
      - name: INSTALL AWS CLIENT
        run: pip install awscli
      - name: INSTALL E BUILD
        run: npm install && npm run build
        env:
            CI: "false"
      - name: Deploy s3
        run: cd build && AWS_ACCESS_KEY_ID=AKIA3CMWG6YOTAFUY3VN 
                          AWS_SECRET_ACCESS_KEY=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  
                              aws s3 sync . s3://wanderson-zap --acl public-read --delete
        
        
        
